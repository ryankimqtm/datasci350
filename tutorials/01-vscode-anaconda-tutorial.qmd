---
title: "DATASCI 350 - Installing VS Code and Connecting it with Anaconda"
lang: en-GB
author: 
  - Danilo Freire
appendix: false
fontfamily: libertine
monofont: inconsolata
monofontoptions: scaled=.95
fontsize: 12pt
spacing: double
geometry: 
  - top=2cm
  - bottom=2cm
  - left=2cm
  - right=2cm
urlcolor: darkblue
linkcolor: Mahogany
citecolor: Mahogany
jupyter: python3
highlight-style: arrow
pdf-engine: pdflatex
format: 
    pdf:
      template: article-template.latex
      toc: true
      toc-depth: 1
      number-sections: true
editor:
  render-on-save: true
---


\newpage

# Introduction

This tutorial installs Visual Studio Code, installs Anaconda, and connects the two. It assumes you have never written a line of code. Every step has a screenshot. If a step fails, email me (<danilo.freire@emory.edu>) or ask one of the teaching assistants.

# Why VS Code?

VS Code is a free code editor made by Microsoft. In the [2025 Stack Overflow Developer Survey](https://survey.stackoverflow.co/2025/technology), 75.9% of developers reported using it, more than twice the share of any other editor. Three reasons matter for this course.

- It does what an editor should do, with syntax highlighting, autocompletion, and a debugger built in. Hundreds of extensions cover anything missing, including [GitHub Copilot](https://github.com/features/copilot/).
- It runs on Windows, macOS, and Linux, so everyone in the class works with the same screen.
- It works with Git and GitHub out of the box, which you will need for every assignment.

# Download and install VS Code

Open <https://code.visualstudio.com/>. Click "Download" for your operating system.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-download.png}
    \caption{The VS Code website.}
    \label{fig:vscode-download}
\end{figure}

## Windows

Run the `VSCodeUserSetup-{version}.exe` file.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/windows01.png}
    \caption{The Windows installer.}
    \label{fig:windows01}
\end{figure}

Accept the licence agreement. Click "Next".

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/windows02.png}
    \caption{The licence agreement.}
    \label{fig:windows02}
\end{figure}

Keep the default installation location. Click "Next".

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/windows03.png}
    \caption{The installation location.}
    \label{fig:windows03}
\end{figure}

Select "Open with Code" under additional tasks. Click "Next".

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/windows05.png}
    \caption{Additional tasks.}
    \label{fig:windows05}
\end{figure}

Click "Install".

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/windows06.png}
    \caption{Installation in progress.}
    \label{fig:windows06}
\end{figure}

## macOS

Open the downloaded `.zip` file.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{images/macos01.png}
    \caption{Unzip the downloaded file.}
    \label{fig:macos01}
\end{figure}

Drag the VS Code application to the Applications folder.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/macos02.png}
    \caption{Drag VS Code to the Applications folder.}
    \label{fig:macos02}
\end{figure}

## Linux

Follow the instructions for your distribution on the [VS Code download page](https://code.visualstudio.com/Download).

## Start VS Code

Open VS Code. The welcome screen appears.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-screen.png}
    \caption{The VS Code welcome screen.}
    \label{fig:vscode-welcome}
\end{figure}

Microsoft publishes short [introductory videos](https://code.visualstudio.com/docs/introvideos/basics) and full [documentation](https://code.visualstudio.com/docs) if you want to explore further.

# Installing Anaconda

Anaconda is a distribution of Python. It ships with the packages this course uses, and it keeps the Python for one project separate from the Python for another.

## Download Anaconda

Open <https://www.anaconda.com/download>. You can skip the registration.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda01.png}
    \caption{The Anaconda website.}
    \label{fig:anaconda-website}
\end{figure}

Click "Download" for your operating system.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda02.png}
    \caption{Download Anaconda.}
    \label{fig:anaconda-download}
\end{figure}

## Windows

Select "Just Me" as the installation type.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda-windows01.png}
    \caption{Installation type.}
    \label{fig:anaconda-install}
\end{figure}

Keep the default installation location.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda-windows02.png}
    \caption{Installation location.}
    \label{fig:anaconda-install02}
\end{figure}

The installer marks the next two options in red and advises against them. Select them anyway. They make the terminal commands in this course work without extra configuration.

Under "Advanced Options", tick "Add Anaconda to my PATH environment variable".

Tick "Register Anaconda as my default Python".

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda-windows03.png}
    \caption{Advanced options.}
    \label{fig:anaconda-advanced}
\end{figure}

## macOS

Open the downloaded `.pkg` file. You can install Anaconda for all users.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda-macos01.png}
    \caption{The macOS installer.}
    \label{fig:anaconda-macos}
\end{figure}

Accept the default option on each screen.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/anaconda-macos02.png}
    \caption{Installation in progress.}
    \label{fig:anaconda-macos02}
\end{figure}

## Linux

Follow the instructions for your distribution on the [Anaconda website](https://docs.anaconda.com/anaconda/install/linux/).

## Verify the installation

Open a terminal. In VS Code, click "Terminal" > "New Terminal".

Type `conda --version`. Press Enter. The Conda version number appears.

Type `python --version`. Press Enter. The version of Python that Anaconda installed appears.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{images/anaconda-verify.png}
    \caption{Both version numbers printed in the VS Code terminal.}
    \label{fig:anaconda-verify}
\end{figure}

If either command reports that it is not found, Anaconda is installed but your terminal cannot see it. Close the terminal and open a new one. If the error persists, reinstall Anaconda and check the PATH option described above.

# Connecting VS Code with Anaconda

Both programs are now installed. The next step tells VS Code to run Anaconda's Python rather than any other Python on your machine.

## Install the Python and Jupyter extensions

Click the Extensions icon in the left sidebar. It looks like four squares.

Type "Python" in the search box.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-python-extension.png}
    \caption{The Python extension in VS Code.}
    \label{fig:vscode-python-extension}
\end{figure}

Install the [official Python extension](https://marketplace.visualstudio.com/items?itemName=ms-python.python) by Microsoft.

Install the [Jupyter extension](https://marketplace.visualstudio.com/items?itemName=ms-toolsai.jupyter) as well. Tutorial 02 needs it.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-jupyter-extension.png}
    \caption{The Jupyter extension in VS Code.}
    \label{fig:vscode-jupyter-extension}
\end{figure}

## Select the Anaconda interpreter

Click "New File" in the top left corner. Save the file with a `.py` extension.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-new-file.png}
    \caption{A new Python file in VS Code.}
    \label{fig:vscode-new-file}
\end{figure}

Type this code in the file:

```{python}
#| eval: false
#| output: false
import sys
print(sys.version)
print(sys.executable)
```

Save the file again. I saved mine as `testing-anaconda.py`.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-save-file.png}
    \caption{Saving the Python file.}
    \label{fig:vscode-save-file}
\end{figure}

Press `Ctrl+Shift+P` (Windows and Linux) or `Cmd+Shift+P` (macOS). The Command Palette opens.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-command-palette.png}
    \caption{The Command Palette in VS Code.}
    \label{fig:vscode-command-palette}
\end{figure}

Type "Python: Select Interpreter". Select it from the list.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-select-interpreter.png}
    \caption{Selecting the Python interpreter.}
    \label{fig:vscode-select-interpreter}
\end{figure}

Select the Anaconda interpreter. It is labelled something like "Python 3.x.x ('base') Conda".

## Verify the connection

Click "Run" in the top right corner of the editor. Select "Run Current File in Dedicated Terminal".

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-run-file.png}
    \caption{Running a Python file in VS Code.}
    \label{fig:vscode-run-file}
\end{figure}

The output shows the Anaconda Python version and the path to it. The word `anaconda` should appear in that path. If it does not, VS Code is running a different Python, and you should select the interpreter again.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/vscode-output.png}
    \caption{The version and path printed in the terminal.}
    \label{fig:vscode-output}
\end{figure}

# Optional: GitHub Copilot

[GitHub Copilot](https://github.com/features/copilot) is an AI assistant built into VS Code. It suggests code as you type and answers questions in a chat panel. The [GitHub tutorial](03-github-tutorial.pdf) covers it in more detail, and Microsoft documents it at <https://code.visualstudio.com/docs/copilot/getting-started-chat>. The short version follows.

Sign up for the free student licence at <https://github.com/education/students>. Click "Join GitHub Education" and follow the instructions to verify your student status. You will need proof of enrolment, such as your Emory email address or a transcript. The licence lasts two years.

Search for [GitHub Copilot](https://marketplace.visualstudio.com/items?itemName=GitHub.copilot) in the Extensions view. Click "Install". VS Code installs two extensions, Copilot and Copilot Chat. Sign in to your GitHub account when prompted.

Press `Tab` to accept a suggestion. Use the chat panel, the speech bubble icon, to ask questions in plain English and get code back.

Copilot is wrong often enough to matter, so read every suggestion before you accept it. Under the course policy, any errors that come from an AI tool are still your responsibility, and you must note in your submission that you used one.

# Conclusion

VS Code and Anaconda are installed and connected, and you can run a Python file from the editor. As your projects grow, create a separate Conda environment for each one so their packages cannot conflict.

For more:

- VS Code Python tutorial: <https://code.visualstudio.com/docs/python/python-tutorial>
- Anaconda documentation: <https://docs.anaconda.com/>
- VS Code documentation: <https://code.visualstudio.com/docs>

Happy coding!

\nocite{*}
